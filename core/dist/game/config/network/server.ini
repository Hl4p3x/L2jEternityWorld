# ---------------------------------------------------------------------------
# Game Server Settings | Настройки Игрового Сервера
# ---------------------------------------------------------------------------
# Connection settings:
# IP settings of the server register in the file ipconfig.xml
# ----------------------------
# Настройки соединения:
# IP настройки сервера прописываются в файле ipconfig.xml
# По умолчанию: 127.0.0.1 
LoginHost = 127.0.0.1

# По умолчанию: 9014
LoginPort = 9014

# По умолчанию: *
GameserverHostname = *

# По умолчанию: 7777
GameserverPort = 7777


# ---------------------------------------------------------------------------
# Database Settings | Настройки подключения к базе данных
# ---------------------------------------------------------------------------
# Specify the appropriate driver and url for the database you're using.
# ----------------------------
# Уберите # перед соответствующим драйвером URL для базы данных. Если вы не знаете что это такое ---> www.google.com или yandex.ru
# Driver = com.mysql.jdbc.Driver (По умолчанию)
# Driver = org.hsqldb.jdbcDriver
# Driver = com.microsoft.sqlserver.jdbc.SQLServerDriver
Driver = com.mysql.jdbc.Driver

# Database URL
# URL = jdbc:mysql://localhost/l2jdb (По умолчанию)
# URL = jdbc:sqlserver://localhost/database = l2jdb/user = sa/password = 
# For english nickname : URL = jdbc:mysql://localhost/l2jgs
# Для русских ников и названий кланов: URL = jdbc:mysql://localhost/l2jdb?useUnicode=true&characterEncoding=utf-8
URL = jdbc:mysql://localhost/l2jdb?useUnicode=true&characterEncoding=utf-8

# Database user info (default is "root" but it's not recommended)
# ----------------------------
# База данных - Информация о пользователе (с использованием корневого (root) пользователя не рекомендуется).
Login = root

# Database connection password
# ----------------------------
# Пароль к базе данных.
Password = 

# По умолчанию: 100
MaximumDbConnections = 100

# По умолчанию: 0
MaximumDbIdleTime = 0


# ---------------------------------------------------------------------------
# Misc Server Settings | Прочие Настройки
# ---------------------------------------------------------------------------
# This is the server ID that the Game Server will request.
# Example: 1 = Bartz
# ----------------------------
# Это ID сервера, который будет запрашивать игровой сервер.
# Примечание: 1 = Bartz
# По умолчанию: 1
RequestServerID = 1

# True = The Login Server will give an other ID to the server if the requested ID is already reserved.
# ----------------------------
# Если опция включена, Логин сервер даст другой ID Игровому серверу.
# По умолчанию: True
AcceptAlternateID = True

# Datapack root directory.
# ----------------------------
# Дирректория Datapack.
DatapackRoot = .

# Define how many players are allowed to play simultaneously on your server.
# ----------------------------
# Максимальное количество игроков, которые смогут играть на сервере.
# По умолчанию: 100
MaximumOnlineUsers = 100

# Numbers of protocol revisions that server allows to connect.
# ----------------------------
# Игровые протоколы, которые разрешены для подключения к серверу.
# По умолчанию: 267;268;271;273
AllowedProtocolRevisions = 267;268;271;273


# ---------------------------------------------------------------------------
# Misc Player Settings | Прочие Игровые Настройки
# ---------------------------------------------------------------------------
# Character name template.
# Examples:
# CnameTemplate = [A-Z][a-z]{3,3}[A-Za-z0-9]*
# The above setting will allow names with first capital letter, next three small letters,
# and any letter (case insensitive) or number, like OmfgWTF1
# CnameTemplate = [A-Z][a-z]*
# The above setting will allow names only of letters with first one capital, like Omfgwtf
# Default .* (allows any symbol)
# ----------------------------
# Шаблон имен Персонажей.
# По умолчанию .* (разрешены все символы)
# Для создания только русских либо только английских ников (в одном нике нельзя одновременно использовать и анг и рус буквы), используйте .([0-9A-Za-z]{2,16})|([0-9\u0410-\u044f]{2,16})*
CnameTemplate = .*

# This setting restricts names players can give to their pets.
# See CnameTemplate for details
# ----------------------------
# Это урегулирование ограничивает имен, которые может дать своему пету игрок.
# По умолчанию .* (разрешены все символы)
# Для создания только русских либо только английских ников (в одном нике нельзя одновременно использовать и анг и рус буквы), используйте .([0-9A-Za-z]{2,16})|([0-9\u0410-\u044f]{2,16})*
PetNameTemplate = .*

# This setting restricts clan/subpledge names players can set.
# See CnameTemplate for details
# ----------------------------
# Шаблон названия кланов.
# По умолчанию .* (разрешены все символы)
# Для создания только русских либо только английских ников (в одном нике нельзя одновременно использовать и анг и рус буквы), используйте .([0-9A-Za-z]{2,16})|([0-9\u0410-\u044f]{2,16})*
ClanNameTemplate = .*

# Maximum number of characters per account.
# ----------------------------
# Максимальное число персонажей на аккаунте.
# По умолчанию: 7 (лимит клиента)
CharMaxNumber = 7